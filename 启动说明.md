# Veil Local Assistant 1.4.1 启动说明

## 直接打开

推荐使用 `.dmg`：打开镜像，把“Veil 本地对话”拖入“应用程序”，以后直接双击应用图标即可，不需要 npm 命令。

当前安装包是 Apple 芯片版本，适用于 M1、M2、M3、M4 系列 Mac。由于当前构建使用本地临时签名，macOS 首次阻止打开时，在 Finder 中右键应用并选择“打开”。

## 使用本地 Ollama

1. 启动 Ollama。
2. 打开 Veil，默认会连接 `http://127.0.0.1:11434`。
3. 在顶部选择已安装模型。

检查 Ollama 状态：

```bash
ollama ps
curl http://127.0.0.1:11434/api/tags
```

如需安装模型：

```bash
ollama pull qwen3.5:9b
```

## 使用 Gemini、DeepSeek 或其他 API

1. 点击右上角齿轮，打开“模型与助手设置”。
2. 选择 Google Gemini、DeepSeek 或 OpenAI 兼容。
3. 确认服务地址，填写模型名称和 API Key。
4. 点击“保存并检测”。

API Key 不会写入项目或对话记录。macOS 支持时，密钥会由系统安全存储加密。选择远程 API 后，对话内容会发送给对应服务商。

## 助手窗口

- 点击顶部图钉：进入透明提词器模式，只显示最新回复和输入框并置顶。
- 透明提词器模式下按 `Esc` 或 `Command + Shift + A` 返回完整界面。
- 点击顶部盾牌：手动开关录屏保护。
- 点击历史记录图标：宽窗口折叠侧栏；小窗口打开或关闭历史抽屉。
- 对话右侧按钮：归档、恢复或永久删除记录。
- 设置中可分别调整透明、置顶、录屏保护和系统提示词。

## Thinking 开关

- 默认关闭，优先获得更快响应。
- Ollama 会根据模型能力自动启用或禁用开关。
- Gemini 关闭时映射为低推理强度；部分 Gemini 模型仍会保留少量推理。
- DeepSeek 可明确切换 thinking 启用或禁用。
- 应用只显示思考进度和累计字符数，不保存或展示思考原文。

## 隐私说明

- 对话历史与设置保存在本机应用数据中。
- Ollama 模式只允许连接本机回环地址。
- 远程 API 地址必须使用 HTTPS，本机兼容服务可使用 HTTP。
- 录屏保护受 macOS 和录屏软件实现限制，不应视为 DRM 或绝对防泄漏措施。
